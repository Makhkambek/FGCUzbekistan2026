# Local run (npm start): put these in .env.local
DATABASE_URL=mysql://root@localhost:3306/fgc
SESSION_SECRET=change-me-to-at-least-32-characters-long

# Docker (docker compose): put these in .env — DATABASE_URL is set by compose
MYSQL_ROOT_PASSWORD=change-me
MYSQL_PASSWORD=change-me
